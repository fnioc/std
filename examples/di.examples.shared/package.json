{
  "name": "@rhombus-std/di.examples.shared",
  "private": true,
  "version": "0.0.0",
  "description": "Shared, source-only contracts + service classes wired by both @rhombus-std/di examples. No build step — main/types point at the TypeScript source, which each example compiles in via a relative import.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@rhombus-std/di": "workspace:*"
  }
}
