{
  "name": "@rhombus-std/examples.lib.without-transformer",
  "private": true,
  "version": "0.0.0",
  "description": "Example dependency library authored in the manual di dialect (explicit tokens + plain-data signatures). Depends on @rhombus-std/di.core and @rhombus-std/primitives alone — no resolution engine — which is the property it exists to prove. Gets a real build for consistency and genuine consumption; ordinary source-libs conditions are fine.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun x tsc -p tsconfig.json",
    "lint": "bun x tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@rhombus-std/di.core": "workspace:*",
    "@rhombus-std/examples.contracts": "workspace:*",
    "@rhombus-std/primitives": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
