{
  "name": "@rhombus-std/examples.app.shared",
  "private": true,
  "version": "0.0.0",
  "description": "The composition-root chapters both example apps run unchanged: the dialect-independent error and manifest-surface tours, and the IServiceProviderFactory both infrastructure chapters build through. It references @rhombus-std/di (the resolution engine), which is exactly what makes it an entrypoint-family package rather than a library.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "bun": "./src/index.ts",
      "types": "./src/index.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun x tsc -p tsconfig.json",
    "lint": "bun x tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@rhombus-std/di": "workspace:*",
    "@rhombus-std/di.core": "workspace:*",
    "@rhombus-std/examples.lib.without-transformer": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
