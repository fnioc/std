{
  "//": "The ttsc/Go lowering config read by @ttsc/unplugin/bun during the Bun.build JS emit (build.ts): every service type, dependency slot and implementer type in this package is derived with `typefor` — from a type argument where a type names it, from the implementer value where a class or factory carries the signatures — so nothing here runs until it is folded. Stage selection is declare-by-depending, resolved HOST-SIDE: there is no `plugins` array, so ttsc auto-discovery spawns the one owner host (transforms/cmd/ttsc-std) from this lib's direct di.extras devDep, and the host runs its whole always-on stage table on every file. Extends the base tsconfig. The `types` augmentation is RETAINED even though nothing here calls di.extras's sugar overloads directly: this package's own program still needs di.extras's `declare module '@rhombus-std/di.core'` augment in scope for the primitive stages' declaration-site matching against `Manifest` to anchor correctly. Lint and d.ts emit stay on plain tsc via tsconfig.json.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": ".ttsc-out"
  }
}
