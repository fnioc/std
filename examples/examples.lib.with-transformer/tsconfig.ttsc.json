{
  "//": "The ttsc/Go lowering config read by @ttsc/unplugin/bun during the Bun.build JS emit (build.ts): add-with-transformer-examples.ts, infrastructure-greeting-workshop.ts and types.ts all mint their service types with `typefor<T>()`, passed as an explicit argument to addClass/addFactory alongside a hand-written dependency-signature array and a plain positional scope string — there is no `.as<>()` chain anywhere in this lib. LocatorGreetingWorkshop resolves with the explicit `getService(typefor<T>())` / `getRequiredService(typefor<T>())` calls, not a tokenless resolve/tryResolve/isService form. Stage selection is declare-by-depending, resolved HOST-SIDE: there is no `plugins` array, so ttsc auto-discovery spawns the one owner host (transforms/cmd/ttsc-std) from this lib's direct di.extras devDep, and the host self-selects the full stage set from its own dependency scan — the generic inline stage plus the primitive stages inline/typefor/schemaof/mergesynth reached through di.extras's honest primitives.extras dep; mergesynth is inert here (this lib installs no augmentation). Extends the base tsconfig. The `types` augmentation is RETAINED even though nothing here calls di.extras's `addClass<T>()`/`addFactory<T>()` sugar overloads directly: this package's own program still needs di.extras's `declare module '@rhombus-std/di.core'` augment in scope for the primitive stages' declaration-site matching against `Manifest` to anchor correctly. Lint and d.ts emit stay on plain tsc via the base tsconfig.json.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["@rhombus-std/di.extras"],
    "rootDir": "./src",
    "outDir": ".ttsc-out"
  }
}
