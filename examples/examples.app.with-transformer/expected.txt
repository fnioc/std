dbug: Rhombus.Hosting.Host[1]
      Hosting starting
info: Rhombus.Examples.InteropWorker[0]
      starting
info: Rhombus.Examples.InteropWorker[0]
      start
=== @rhombus-std interop — with transformer ===
async banner: == @rhombus-std interop demo ==
greetings aggregated from both libraries (2):
  lib.with-transformer: Good day, Ada.!
  lib.without-transformer: Hey Ada!
server options (live): Host=0.0.0.0 Port=8080 MaxConnections=100
greeting policy excitement (static wrap): "!"
health check present: true (ok)
live reload (config → reactive Options):
  MaxConnections before reload: 100
  reload fired: MaxConnections is now 250
  MaxConnections after reload: 250
info: Rhombus.Examples.InteropWorker[0]
      started
dbug: Rhombus.Hosting.Host[2]
      Hosting started
dbug: Rhombus.Hosting.Host[3]
      Hosting stopping
info: Rhombus.Examples.InteropWorker[0]
      stopping
info: Rhombus.Examples.InteropWorker[0]
      stop
info: Rhombus.Examples.InteropWorker[0]
      stopped
dbug: Rhombus.Hosting.Host[4]
      Hosting stopped

=== di registration — with transformer ===
immutability: the discarded call registered 0, the threaded one registered 1
defaults: applying them twice leaves 1 sink (tryAdd only registers what is missing)
defaults: an application that registered its own sink keeps it (recording)
override: replace swapped all three defaults; the host sink is recording, and 1 registration is left at its type
teardown: removeAll left 0 sinks on the new manifest, and 1 on the original (nothing mutates)
notify: [production] 2024-05-01T09:00:00Z order-42 shipped
audit: 1 entry, sink echo enabled=true
keyed sink (key "email"): to:orders@example.test 2024-05-01T09:00:00Z welcome
keyed value (key "vendor"): 2024-05-01T09:00:00Z
2 sinks share the IMessageSink type; the most recently registered one wins a single request, and all of them answer a collection request (2)
described by chain: [staging] 2024-05-01T09:00:00Z order-99 shipped
fork: removeAll left 0 sinks (the original still has 2), so the audit log's optional sink slot resolved to undefined and it recorded 1 entry anyway

=== di resolution — with transformer ===
required vs optional lookup
  resolve(IPaymentRouter): resolved PaymentRouter
  resolve(IFraudScreen): UnsatisfiableError — a required miss is loud
  resolve(IAuditTrail): present
  resolve(IFraudScreen): undefined
  resolve(IFraudScreen) !== undefined: false
collection resolution — 3 registrations share one type, all of them run
  A-1001: total-within-limit → ok
  A-1001: amount-is-positive → ok
  A-1001: method-is-configured → ok
  X-9009: total-within-limit → over the 250000 limit
  X-9009: amount-is-positive → ok
  X-9009: method-is-configured → no gateway for "crypto"
  the same aggregation asked for as a type: 3 validators
keyed resolution — one base type, three keys
  A-1001 checkout (key "wallet"): wallet debited 4250 for A-1001 → #1001 A-1001 via wallet
  B-2002 checkout (key "card"): card authorised 990 for B-2002 → #1001 B-2002 via card
  resolve at key "crypto": undefined
  a keyed registration is not in the bare base's collection: 0 gateways
factory slots — the caller supplies what the container cannot know
  mint C-3003: invoice raised 12000 for C-3003 → #1001 C-3003 via invoice
  asking the provider for one: #1001 C-3003 via invoice
async registrations — the promise is the registration
  rates as of 2026-01-01, EUR at 0.85
  the bare type has no registration: undefined
  resolveAsync unwraps the same registration directly: EUR at 0.85
the provider as a service — usually a smell, occasionally correct
  the injected view is a view onto it, not the container object: true
optional audit trail: 2 entries, last B-2002

=== di open generics — with transformer ===
IRepository<$1> is registered ONCE; every closing below is minted from it:
  IRepository<User>: User -> u-1, u-2
  IRepository<Order>: Order -> o-1, o-2, o-3
the closing propagates down the graph — IRepository<T> -> ITable<T> -> Seed<T>:
  ITable<User> reports the closing it was minted for: User
  IRepository<User>.all() is the array registered as Seed<User>: true
a CLOSED registration serves the one closing it names:
  IRepository<AuditEvent>: AuditEvent -> 2 event(s), ids redacted
arity 2 — $1 and $2 close independently, each side keeping its own precedence:
  IJoin<User,AuditEvent>: User (2) joined with AuditEvent (2)
a template may pin some arguments; where two overlap, the later registration wins:
  IJoin<Order,User> goes to the pinned IJoin<Order,$2>: Order (3, total 66) joined with User (2)
  IJoin<AuditEvent,User> goes to the general IJoin<$1,$2>: AuditEvent (2) joined with User (2)
the template itself is NOT resolvable — a hole is not a service:
  asking for IRepository<$1> was refused (UnsatisfiableError)

=== di errors (dialect-independent) ===
keying a service type that already carries a key: Error — not a container failure; this diagnostic would rethrow rather than guess
building with validateBuildability: ManifestValidationError — 1 registration(s) cannot be satisfied: selfcheck:IStore
  the failure inside it: UnsatisfiableError: cannot satisfy selfcheck:IStore — it is registered, but something it needs is not; register what is missing, or ask through the union address if its absence is legitimate
asking at the bare address for a registration that cannot be built: UnsatisfiableError: cannot satisfy selfcheck:IStore — it is registered, but something it needs is not; register what is missing, or ask through the union address if its absence is legitimate
asking for a type nobody registered: UnsatisfiableError: cannot satisfy selfcheck:IReportService — nothing in the manifest produces it; register what is missing, or ask through the union address if its absence is legitimate
a dependency cycle: CycleError — selfcheck:ILedger -> selfcheck:IAuditLog -> selfcheck:ILedger; break the loop with a factory slot
every failure above shares one root: true
something else entirely: TypeError — not a container failure; this diagnostic would rethrow rather than guess

=== di infrastructure (library-author surface) — with transformer ===
app registered no stationery:
  stationery overridden: false
  card: -- Hello, Ada --
app registered its own stationery:
  stationery overridden: true
  card: *** Hello, Grace ***
the same card, two ways to reach its dependencies:
  parameters (GreetingWorkshop): -- Hello, Linus --
  injected provider (LocatorGreetingWorkshop): -- Hello, Linus --
asking optionally for an unregistered type: undefined
building a graph with a hole in it: caught a DiError (ManifestValidationError)
