{
  "//": "Plain tsc — no transformer. Every token is hand-written, so nothing needs lowering. Workspace packages resolve to source, so importing @rhombus-std/options.augmentations declaration-merges addOptions/configure onto di.core's Manifest interface directly in this program. module:ESNext keeps specifiers verbatim in the emitted JS; the app runs under bun, whose preload lowers the source-resolved workspace deps at load time. The DOM lib supplies console.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "rootDir": "src",
    "outDir": "dist",
    "lib": [
      "ESNext",
      "DOM"
    ],
    "declaration": false,
    "declarationMap": false,
    "sourceMap": true,
    "composite": false
  },
  "include": [
    "src/**/*"
  ]
}
