=== @rhombus-std/di — without transformer ===
greeter is a shared singleton: true
Greeter instances built: 1
ConsoleLogger instances built: 3
SystemClock instances built: 2
logged lines:
  [2026-01-01T00:00:00Z] Hello, Ada!
  [2026-01-01T00:00:00Z] Hello, Linus!
  [union] union-test
  diagnostics at 2026-01-01T00:00:00Z
  [sql] saved app/User
  [sql] saved app/Invoice
  Welcome! (as of 2026-01-01T00:00:00Z)
  personalized greeting requested for Bob
  personalized greeting requested for Carol
  disposed: sync-resource
  disposed: async-resource
  disposed: sync-resource
request 1 id stable within scope: true (value 1)
request 2 id is distinct: true (value 2)
RequestId instances built: 2
union resolved to logger (first in union): true
inject brand pinned correct clock: true
overloaded signatures:
  richer overload chosen when its deps are present: true
  falls back to the leaner overload when a dep is absent: true
open generics:
  user repo is a per-closing singleton: true
  distinct closings are distinct instances: true
  user save: [sql] saved app/User
  invoice save: [sql] saved app/Invoice
  SqlRepository instances built: 2
  closed registration wins for Order: memory
  order save: [memory] saved app/Order (count 1)
  auditor closed over the user repo: true (auditing app/User)
scopes — a third custom tag, and TRANSIENT (no matching open frame):
  operation id stable within one frame: true (value 1)
  operation id distinct across sibling frames: true (value 2)
  same registration, no open "operation" frame here => transient: true
factory registration — addFactory(token, fn, [[...]]):
  welcome banner built via factory: Welcome! (as of 2026-01-01T00:00:00Z)
addValue(token, v) — a pre-built instance, no construction step:
  resolving twice trivially returns the identical reference: true (demo@1.0.0)
FactoryRef slot — a ctor param injected with a callable, not a resolved instance: true
Typeof<T> witness — the erased SqlRepository knows its own closing: app/User
literal dep — channel injected via a hand-written { value: "stable" } slot, no token or registration: true
optional dep — unregistered FEATURE_TOGGLE degrades to undefined, never throws: no toggle registered (undefined fallback)
resolveFactory — zero-arg (respects lifetime) vs parameterized (always fresh):
  zero-arg factory returns the SAME cached singleton: true
  parameterized factory builds fresh every call: true
  Welcome, Bob! / Welcome, Carol!
ResolveScope (Resolver) injection — factory resolved its own deps imperatively: [demo@1.0.0] as of 2026-01-01T00:00:00Z
=== HEADLINE: async — Promise<T>-only registration + recursive resolveAsync ===
  resolveAsync(REMOTE_CONFIG) directly: https://api.example.test
  resolveAsync(REMOTE_CONFIG_CONSUMER) recursively resolved the bare token via the Promise<T> fallback: resolved via async fallback: endpoint=https://api.example.test
  RemoteConfig built exactly once — shared singleton cache through the async fallback: true
disposal — Symbol.dispose (sync) vs Symbol.asyncDispose (async):
  sync dispose() tore down the sync-only scope: true
  disposeAsync() tore down both, REVERSE of construction order: true
  disposal log order: disposed: async-resource then disposed: sync-resource
last-wins override (registering a token twice — the engine keeps both, resolves the most recent):
  bare CLOCK now resolves the override: true (2099-12-31T23:59:59Z)
=== HEADLINE: class-as-factory via a hand-written overload-tuple wrapper ===
  addFactory(overload-faithful wrapper) resolves identically to add(Reporter): true (both: "with-metrics")
  Reflect.construct really built a genuine Reporter: true
