{
  "name": "@rhombus-std/di.examples.abstractions-lib",
  "private": true,
  "version": "0.0.0",
  "description": "The abstractions smoke test: a library that contributes DI registrations depending ONLY on @rhombus-std/di.core (import type) — no @rhombus-std/di runtime. Proves a lib author needs only the .d.ts abstractions.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@rhombus-std/di.core": "workspace:*"
  }
}
