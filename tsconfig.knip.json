{
  "//": "Resolution-only tsconfig, read by `bun run knip` (`--tsConfig tsconfig.knip.json`) and by NOTHING else -- it is not a build, gate, or editor program. In-repo exports resolve @rhombus-std/* to src for every condition set, oxc-resolver's require/import/node/default included, so the scan reads the same on a cold clone and after `bun run build` with no `paths` help.",
  "extends": "./tsconfig.base.json",
  "files": [],
  "include": []
}
