{
  "name": "@rhombus-std/logging.core.test",
  "private": true,
  "version": "0.0.0",
  "description": "Tests for @rhombus-std/logging.core. Not published.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@rhombus-std/logging": "workspace:*",
    "@rhombus-std/logging.core": "workspace:*",
    "@rhombus-std/primitives": "workspace:*"
  }
}
