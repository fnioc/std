{
  "name": "@rhombus-std/di.extras.test",
  "private": true,
  "version": "0.0.0",
  "description": "Compile-time regressions for the @rhombus-std/di.extras authoring augmentation — assertions that only hold in a program where the declare-module merge is in scope. Not published.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@rhombus-std/di2": "workspace:*",
    "@rhombus-std/di2.core": "workspace:*",
    "@rhombus-std/di.extras": "workspace:*"
  }
}
