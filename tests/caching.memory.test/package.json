{
  "name": "@rhombus-std/caching.memory.test",
  "private": true,
  "version": "0.0.0",
  "description": "Tests for @rhombus-std/caching.memory. Not published.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@rhombus-std/caching.core": "workspace:*",
    "@rhombus-std/caching.memory": "workspace:*",
    "@rhombus-std/di": "workspace:*"
  }
}
