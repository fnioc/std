{
  "name": "@rhombus-std/config.transformer.test",
  "private": true,
  "version": "0.0.0",
  "description": "Tests for @rhombus-std/config.transformer. Not published.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@rhombus-std/config": "workspace:*",
    "@rhombus-std/config.core": "workspace:*",
    "@rhombus-std/config.transformer": "workspace:*",
    "ts-patch": "^3.3.0"
  }
}
