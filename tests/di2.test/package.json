{
  "name": "@rhombus-std/di2.test",
  "private": true,
  "version": "0.0.0",
  "description": "Tests for @rhombus-std/di2 and @rhombus-std/di2.core. Not published.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@rhombus-std/di2": "workspace:*",
    "@rhombus-std/di2.core": "workspace:*",
    "@rhombus-std/primitives": "workspace:*"
  }
}
