{
  "name": "@rhombus-std/logging.browserconsole.test",
  "private": true,
  "version": "0.0.0",
  "description": "Tests for @rhombus-std/logging.browserconsole. Not published.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@rhombus-std/di.core": "workspace:*",
    "@rhombus-std/logging": "workspace:*",
    "@rhombus-std/logging.browserconsole": "workspace:*",
    "@rhombus-std/logging.core": "workspace:*"
  }
}
