{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "composite": false,
    // The type assertions index into overload tuples; relaxing
    // noUncheckedIndexedAccess keeps them readable without `!` on every lookup.
    "noUncheckedIndexedAccess": false
  },
  "include": ["test/**/*"]
}
