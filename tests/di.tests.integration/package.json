{
  "name": "@rhombus-std/di.tests.integration",
  "private": true,
  "version": "0.0.0",
  "description": "End-to-end integration tests: transformer-compiled output running against the di engine, plus progressive-enhancement parity. Not published.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@rhombus-std/di.core": "workspace:*",
    "@rhombus-std/di": "workspace:*",
    "@rhombus-std/di.transformer": "workspace:*",
    "ts-patch": "^3.3.0"
  }
}
