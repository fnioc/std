{
  "name": "@rhombus-std/di.tests.integration",
  "private": true,
  "version": "0.0.0",
  "description": "Compile-time type-utility checks for di.core's OverloadedParameters / OverloadedConstructorParameters. Not published.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@rhombus-std/di.core": "workspace:*"
  }
}
