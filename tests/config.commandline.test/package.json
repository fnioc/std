{
  "name": "@rhombus-std/config.commandline.test",
  "private": true,
  "version": "0.0.0",
  "description": "Tests for @rhombus-std/config.commandline. Not published.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@rhombus-std/config.commandline": "workspace:*"
  }
}
