{
  "//": "Lint-time typecheck config for the integration tests (the `lint` script's target; `bun test` runs them). tsc resolves each @rhombus-std/config.* import to src, so the provider `declare module` augmentations enter this program and the add* members typecheck on ConfigBuilder.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "types": ["bun"],
    "composite": false,
    "declaration": false,
    "declarationMap": false,
    "sourceMap": false
  },
  "include": ["test/**/*"]
}
