{
  "name": "@rhombus-std/di.test",
  "private": true,
  "version": "0.0.0",
  "description": "Tests for @rhombus-std/di and @rhombus-std/di.core. Not published.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "bun run test:types && bun test",
    "test:types": "tsc --noEmit -p tsconfig.types.json",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@rhombus-std/di": "workspace:*",
    "@rhombus-std/di.core": "workspace:*",
    "@rhombus-std/primitives": "workspace:*",
    "@rhombus-toolkit/obj": "^3.0.1"
  }
}
