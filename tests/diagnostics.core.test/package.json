{
  "name": "@rhombus-std/diagnostics.core.test",
  "private": true,
  "version": "0.0.0",
  "description": "Tests for @rhombus-std/diagnostics.core. Not published.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@rhombus-std/di": "workspace:*",
    "@rhombus-std/di.core": "workspace:*",
    "@rhombus-std/diagnostics": "workspace:*",
    "@rhombus-std/diagnostics.core": "workspace:*"
  }
}
