{
  "name": "@rhombus-std/logging.test",
  "private": true,
  "version": "0.0.0",
  "description": "Tests for @rhombus-std/logging (the filter/selection engine, scope machinery, LoggerFactory). Not published.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@rhombus-std/di": "workspace:*",
    "@rhombus-std/di.core": "workspace:*",
    "@rhombus-std/logging": "workspace:*",
    "@rhombus-std/logging.core": "workspace:*",
    "@rhombus-std/options": "workspace:*",
    "@rhombus-toolkit/func": "3.5.3"
  }
}
