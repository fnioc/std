{
  "name": "@rhombus-std/di.registration.ttsc.e2e",
  "private": true,
  "version": "0.0.0",
  "description": "End-to-end byte-parity harness for the registration authoring surface: the type-driven addClass<I>(C) / addFactory<I>(fn) / addValue<I>(v) sugar must emit exactly what a hand-writer spells, and a source-written typefor must derive the implementation type the real verbs accept. Not published.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test:e2e": "bun test"
  },
  "devDependencies": {
    "@rhombus-std/di.core": "workspace:*",
    "@rhombus-std/di.extras": "workspace:*",
    "@rhombus-std/primitives": "workspace:*",
    "@rhombus-std/primitives.extras": "workspace:*",
    "@ttsc/unplugin": "^0.18.1",
    "ttsc": "^0.18.1",
    "typescript": "^7.0.2"
  }
}
