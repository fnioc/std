{
  "name": "@rhombus-std/di.signatureof.ttsc.e2e",
  "private": true,
  "version": "0.0.0",
  "description": "End-to-end byte-parity harness for the signatureof primitive: the type-driven add<I>(C) / addFactory<I>(fn) sugar lowered via the inline + nameof + signatureof stages must emit exactly what the di registration stage emits directly. Not published.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test:e2e": "bun test"
  },
  "devDependencies": {
    "@rhombus-std/di.core": "workspace:*",
    "@rhombus-std/di.transformer": "workspace:*",
    "@rhombus-std/primitives": "workspace:*",
    "@rhombus-std/primitives.extras": "workspace:*",
    "@ttsc/unplugin": "^0.18.1",
    "ttsc": "^0.18.1",
    "typescript": "^7.0.2"
  }
}
