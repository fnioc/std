{
  "name": "@rhombus-std/config.core",
  "version": "0.0.0",
  "description": "Types-only configuration abstractions: the IConfiguration* interfaces and ITryGetResult. Zero runtime, zero dependencies.",
  "keywords": [
    "configuration",
    "config",
    "abstractions",
    "types",
    "typescript"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/fnioc/std.git",
    "directory": "libraries/config.core"
  },
  "type": "module",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "bun": "./src/index.ts",
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build.ts",
    "lint": "bun x tsc --noEmit -p tsconfig.json"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true,
    "types": "./dist/index.d.ts",
    "exports": {
      ".": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.d.ts"
      }
    }
  }
}
