{
  "//": "Lint-time typecheck config: identical to tsconfig.json but WIDENED to include test/**/*. `bun test` never type-checks anything (bun runs TS directly), and the base tsconfig.json used by `build` only compiles src/**/*. rootDir widens to '.' since test/ lives outside src/.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": "."
  },
  "include": ["src/**/*", "test/**/*"]
}
