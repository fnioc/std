{
  "name": "@rhombus-std/di.core",
  "version": "10.1.0",
  "description": "The pure-types ioc abstractions substrate: the dependency-signature data format, the slot/token type surface, and the authoring type machinery a library author depends on to author registrations without the di runtime. Types only — zero runtime, zero dependencies.",
  "keywords": [
    "dependency-injection",
    "di",
    "ioc",
    "typescript"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/fnioc/std.git",
    "directory": "libraries/di.core"
  },
  "type": "module",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun run build.ts",
    "lint": "eslint ."
  },
  "publishConfig": {
    "access": "public",
    "provenance": true,
    "types": "./dist/index.d.ts",
    "exports": {
      ".": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.d.ts"
      }
    }
  },
  "devDependencies": {
    "@rhombus-toolkit/func": "3.5.3"
  }
}
