{
  "name": "@rhombus-std/config",
  "version": "0.0.0",
  "description": "Layered configuration engine: ConfigBuilder/Root/Section, compareConfigKeys, the Memory provider, an index-navigable Section tree, and a Type-tree schema that coerces on build().",
  "keywords": [
    "configuration",
    "config",
    "options",
    "ioc",
    "typescript"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/fnioc/std.git",
    "directory": "libraries/config"
  },
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "sideEffects": true,
  "//config-source": "config self-augments: memory/chained both `declare module \"@rhombus-std/config\"` (the barrel) and import ConfigBuilder from it, so every one of config's own ConfigBuilder augmenters merges onto ONE type -- mixing the barrel with a relative `../ConfigBuilder` phantom-splits the class and `return this` fails. But config's own compile has no built dist to resolve its own name against (TS2664/TS2307 the moment dist is absent). The package-unique `config-source` condition, listed FIRST on the `.` export (ahead of `types`), routes config's OWN program back to ./src/index.ts so the self-`declare module` and self-imports resolve during the not-yet-built compile; every external consumer (which never sets this condition) resolves `types` -> the rolled ./dist/bundle/index.d.ts. This is di.core's `di-core-source` self-source pattern (§72/§74). Dev-only: absent from publishConfig, where `.` collapses onto ./dist/bundle/index.* like the reference. No merge-anchor subpaths remain.",
  "exports": {
    ".": "./src/index.ts",
    "./tokens/*": {
      "types": "./src/*.ts",
      "bun": "./src/*.ts"
    }
  },
  "files": [
    "dist",
    "!dist/stage"
  ],
  "scripts": {
    "build": "bun ../../scripts/build-lib.ts",
    "lint": "bun x tsc --noEmit -p tsconfig.ci.json"
  },
  "dependencies": {
    "@rhombus-std/config.core": "workspace:*",
    "@rhombus-std/primitives": "workspace:*",
    "@rhombus-toolkit/func": "^3.6.0",
    "@rhombus-toolkit/type-guards": "^1.3.4",
    "@rhombus-toolkit/type-helpers": "^2.0.0"
  },
  "devDependencies": {
    "@rhombus-std/primitives.extras": "workspace:*",
    "@rhombus-toolkit/proxy-base": "^1.0.0",
    "@ttsc/unplugin": "^0.18.1",
    "ttsc": "^0.18.1",
    "typescript": "^6.0.3"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true,
    "main": "./dist/bundle/index.js",
    "types": "./dist/bundle/index.d.ts",
    "exports": {
      ".": {
        "types": "./dist/bundle/index.d.ts",
        "import": "./dist/bundle/index.js",
        "default": "./dist/bundle/index.js"
      }
    }
  }
}
