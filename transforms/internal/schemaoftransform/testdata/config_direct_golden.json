{
  "import { ConfigBuilder } from \"@rhombus-std/config\";\ninterface C {\n    flag: boolean;\n}\ndeclare const b: ConfigBuilder;\nexport const viaWithType = b.withType<C>();\nexport const viaSchemaof = schemaof<C>();\n": "import { ConfigBuilder } from \"@rhombus-std/config\";\ninterface C {\n    flag: boolean;\n}\ndeclare const b: ConfigBuilder;\nexport const viaWithType = b.withSchema({\n    flag: \"boolean\"\n});\nexport const viaSchemaof = schemaof<C>();\n",
  "import { ConfigBuilder } from \"@rhombus-std/config\";\ninterface C {\n    host: string;\n    port: number;\n    ssl?: boolean;\n}\ndeclare const b: ConfigBuilder;\nexport const viaWithType = b.withType<C>();\nexport const viaSchemaof = schemaof<C>();\n": "import { OPTIONAL } from \"@rhombus-std/config\";\nimport { ConfigBuilder } from \"@rhombus-std/config\";\ninterface C {\n    host: string;\n    port: number;\n    ssl?: boolean;\n}\ndeclare const b: ConfigBuilder;\nexport const viaWithType = b.withSchema({\n    host: \"string\",\n    port: \"number\",\n    ssl: { [OPTIONAL]: \"boolean\" }\n});\nexport const viaSchemaof = schemaof<C>();\n",
  "import { ConfigBuilder } from \"@rhombus-std/config\";\ninterface C {\n    ssl?: boolean;\n}\ndeclare const b: ConfigBuilder;\nexport const viaWithType = b.withType<C>();\nexport const viaSchemaof = schemaof<C>();\n": "import { OPTIONAL } from \"@rhombus-std/config\";\nimport { ConfigBuilder } from \"@rhombus-std/config\";\ninterface C {\n    ssl?: boolean;\n}\ndeclare const b: ConfigBuilder;\nexport const viaWithType = b.withSchema({\n    ssl: { [OPTIONAL]: \"boolean\" }\n});\nexport const viaSchemaof = schemaof<C>();\n",
  "import { ConfigBuilder } from \"@rhombus-std/config\";\ninterface C {\n    Database: {\n        Primary: {\n            Host: string;\n            PoolSize: number;\n        };\n    };\n}\ndeclare const b: ConfigBuilder;\nexport const viaWithType = b.withType<C>();\nexport const viaSchemaof = schemaof<C>();\n": "import { ConfigBuilder } from \"@rhombus-std/config\";\ninterface C {\n    Database: {\n        Primary: {\n            Host: string;\n            PoolSize: number;\n        };\n    };\n}\ndeclare const b: ConfigBuilder;\nexport const viaWithType = b.withSchema({\n    Database: {\n        Primary: {\n            Host: \"string\",\n            PoolSize: \"number\"\n        }\n    }\n});\nexport const viaSchemaof = schemaof<C>();\n",
  "import { ConfigBuilder } from \"@rhombus-std/config\";\ninterface C {\n    Server: {\n        Host: string;\n        Port: number;\n    };\n}\ndeclare const b: ConfigBuilder;\nexport const viaWithType = b.withType<C>();\nexport const viaSchemaof = schemaof<C>();\n": "import { ConfigBuilder } from \"@rhombus-std/config\";\ninterface C {\n    Server: {\n        Host: string;\n        Port: number;\n    };\n}\ndeclare const b: ConfigBuilder;\nexport const viaWithType = b.withSchema({\n    Server: {\n        Host: \"string\",\n        Port: \"number\"\n    }\n});\nexport const viaSchemaof = schemaof<C>();\n",
  "import { ConfigBuilder } from \"@rhombus-std/config\";\ninterface C {\n    s: string;\n    n: number;\n    b: boolean;\n}\ndeclare const b: ConfigBuilder;\nexport const viaWithType = b.withType<C>();\nexport const viaSchemaof = schemaof<C>();\n": "import { ConfigBuilder } from \"@rhombus-std/config\";\ninterface C {\n    s: string;\n    n: number;\n    b: boolean;\n}\ndeclare const b: ConfigBuilder;\nexport const viaWithType = b.withSchema({\n    s: \"string\",\n    n: \"number\",\n    b: \"boolean\"\n});\nexport const viaSchemaof = schemaof<C>();\n"
}